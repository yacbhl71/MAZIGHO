import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, ArrowLeft, CheckCircle2, LockKeyhole, Minus, PackageCheck, Plus, ShieldCheck, ShoppingBag, Trash2, Truck } from "lucide-react";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import { useCart } from "@/hooks/useCart";
import { trpc } from "@/lib/trpc";
import { formatPrice } from "@/lib/currency";
import { getDeliveryProfileForCountry, useDeliveryCountry } from "@/contexts/DeliveryCountryContext";
import { useLocale } from "@/contexts/LocaleContext";

export default function Cart() {
  const [, setLocation] = useLocation();
  const { cart, updateQuantity, removeFromCart, clearCart } = useCart();
  const { locale } = useLocale();
  const { countryCode, countryLabel } = useDeliveryCountry();
  const productsQuery = trpc.products.getAll.useQuery(locale);
  const products = productsQuery.data || [];
  const productById = new Map(products.map(product => [product.id, product]));
  const items = cart.map(item => {
    const product = productById.get(item.productId);
    const deliveryProfile = getDeliveryProfileForCountry(product?.deliveryProfiles, countryCode);
    const available = Boolean(product && product.stock > 0 && deliveryProfile);
    const reason = !product ? "Cette fiche n’est plus disponible dans la langue ou le catalogue actuel." : product.stock <= 0 ? "Ce produit est actuellement en rupture de stock." : !deliveryProfile ? `Aucun devis de livraison validé pour ${countryLabel}.` : null;
    return { item, product, deliveryProfile, available, reason, shipping: deliveryProfile ? deliveryProfile.customerShippingCost * item.quantity : 0 };
  });
  const subtotal = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const shipping = items.reduce((sum, entry) => sum + entry.shipping, 0);
  const estimatedTotal = subtotal + shipping;
  const unavailableItems = items.filter(entry => !entry.available);
  const isReadyForReview = cart.length > 0 && !productsQuery.isLoading && unavailableItems.length === 0;

  if (cart.length === 0) {
    return <div className="flex min-h-screen flex-col bg-[#fbf7f2]"><Header /><main className="container flex flex-1 items-center justify-center px-4 py-20 text-center"><div className="max-w-md"><div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-orange-100 text-orange-600"><ShoppingBag className="h-9 w-9" /></div><p className="mb-3 text-xs font-bold uppercase tracking-[0.28em] text-orange-600">Votre sélection</p><h1 className="text-3xl font-semibold tracking-tight text-slate-950">Votre panier est vide</h1><p className="mt-4 text-slate-600">Découvrez nos univers et ajoutez vos coups de cœur à votre sélection.</p><Button onClick={() => setLocation("/boutique")} className="mt-8 gap-2 bg-orange-500 text-white hover:bg-orange-600"><ArrowLeft className="h-4 w-4" /> Continuer vos achats</Button></div></main><Footer /></div>;
  }

  return <div className="flex min-h-screen flex-col bg-[#fbf7f2]"><Header /><main className="container flex-1 px-4 py-10 md:py-14"><div className="mb-10 flex flex-col gap-4 border-b border-[#eadfd2] pb-8 md:flex-row md:items-end md:justify-between"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.28em] text-orange-600">Votre sélection</p><h1 className="text-4xl font-semibold tracking-tight text-slate-950">Votre panier</h1><p className="mt-2 text-sm text-slate-600">Les frais et délais ci-dessous reposent sur les devis enregistrés pour <strong>{countryLabel}</strong>.</p></div><Button variant="outline" onClick={() => setLocation("/boutique")} className="w-fit gap-2 border-[#d9cbbc] bg-white"><ArrowLeft className="h-4 w-4" /> Continuer vos achats</Button></div>
  <div className="mb-6 flex items-start gap-3 rounded-xl border border-sky-200 bg-sky-50 p-4 text-sm leading-6 text-sky-950"><Truck className="mt-0.5 h-5 w-5 shrink-0 text-sky-700" /><p><strong>Destination sélectionnée : {countryLabel}.</strong> Chaque frais affiché correspond au produit concerné et à cette destination. Aucun produit sans devis de livraison validé ne peut avancer vers l’étape suivante.</p></div>
  {productsQuery.isLoading && <div className="mb-6 flex items-center gap-3 rounded-xl border bg-white p-4 text-sm text-slate-600"><span className="h-4 w-4 animate-spin rounded-full border-2 border-orange-500 border-t-transparent" /> Vérification des produits et de la livraison…</div>}
  {unavailableItems.length > 0 && <div className="mb-6 flex items-start gap-3 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-950"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-amber-700" /><div><strong>Votre sélection doit être ajustée.</strong><p className="mt-1">{unavailableItems.length} article(s) ne disposent pas d’un profil valide pour {countryLabel}. Retirez-les ou choisissez une autre destination avant de poursuivre.</p></div></div>}
  <div className="grid gap-8 lg:grid-cols-[1.2fr_0.8fr]"><div className="space-y-4">{items.map(({ item, product, deliveryProfile, available, reason }) => <Card key={`${item.productId}-${JSON.stringify(item.options)}`} className={`border bg-white shadow-none ${available ? "border-[#eadfd2]" : "border-amber-200"}`}><CardContent className="p-4 md:p-6"><div className="flex gap-4 md:gap-6"><div className="h-28 w-28 shrink-0 overflow-hidden rounded-xl bg-[#f2eee9] md:h-36 md:w-36">{item.imageUrl ? <img src={item.imageUrl} alt={item.productName} className="h-full w-full object-cover" /> : <div className="flex h-full items-center justify-center text-4xl">📦</div>}</div><div className="flex min-w-0 flex-1 flex-col"><div className="flex justify-between gap-3"><div><p className="mb-1 text-xs uppercase tracking-wider text-slate-400">Article sélectionné</p><h2 className="line-clamp-2 text-base font-semibold text-slate-950 md:text-lg">{item.productName}</h2></div><button onClick={() => removeFromCart(item.productId, item.options)} className="h-fit rounded-full p-2 text-slate-400 hover:bg-red-50 hover:text-red-500" aria-label="Supprimer l'article"><Trash2 className="h-4 w-4" /></button></div>{item.options && Object.keys(item.options).length > 0 && <div className="mt-2 text-xs text-slate-500">{Object.entries(item.options).map(([key, value]) => <span key={key} className="mr-3">{key}: {value}</span>)}</div>}<div className={`mt-3 rounded-lg border p-3 text-xs leading-5 ${available ? "border-emerald-200 bg-emerald-50 text-emerald-900" : "border-amber-200 bg-amber-50 text-amber-950"}`}>{available && deliveryProfile ? <><p className="font-semibold"><CheckCircle2 className="mr-1 inline h-3.5 w-3.5" />Livrable vers {countryLabel}</p><p className="mt-1">{deliveryProfile.customerShippingCost === 0 ? "Livraison incluse dans le prix" : `Frais de livraison : ${formatPrice(deliveryProfile.customerShippingCost, locale)} par article`}{deliveryProfile.minDeliveryDays ? ` · délai estimé ${deliveryProfile.minDeliveryDays}${deliveryProfile.maxDeliveryDays && deliveryProfile.maxDeliveryDays !== deliveryProfile.minDeliveryDays ? `–${deliveryProfile.maxDeliveryDays}` : ""} jours` : ""}</p><p className="mt-1 text-[11px] text-emerald-700">Devis fournisseur vérifié pour cette destination.</p></> : <><p className="font-semibold">Livraison non confirmée vers {countryLabel}</p><p className="mt-1">{reason}</p></>}</div><div className="mt-auto flex flex-wrap items-end justify-between gap-3 pt-4"><div><p className="text-xs text-slate-500">Prix unitaire</p><p className="font-semibold text-slate-950">{formatPrice(item.price, locale)}</p></div><div className="flex items-center rounded-lg border border-[#d9cbbc] bg-[#fbf7f2]"><button onClick={() => updateQuantity(item.productId, item.quantity - 1, item.options)} className="p-2.5 hover:text-orange-600" aria-label="Diminuer la quantité"><Minus className="h-4 w-4" /></button><span className="min-w-9 text-center text-sm font-semibold">{item.quantity}</span><button onClick={() => updateQuantity(item.productId, item.quantity + 1, item.options)} className="p-2.5 hover:text-orange-600" aria-label="Augmenter la quantité"><Plus className="h-4 w-4" /></button></div><p className="text-lg font-bold text-orange-600">{formatPrice(item.price * item.quantity, locale)}</p></div></div></div></CardContent></Card>)}</div>
  <div className="space-y-5"><Card className="border-orange-200 bg-white shadow-none lg:sticky lg:top-24"><CardContent className="p-6 md:p-7"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-semibold text-slate-950">Résumé pour {countryLabel}</h2><span className="rounded-full bg-orange-50 px-3 py-1 text-xs font-semibold text-orange-700">CHF</span></div><div className="mb-5 border border-sky-100 bg-sky-50 p-4 text-sm leading-6 text-sky-900"><strong>Estimation transparente.</strong> Les frais sont additionnés par article selon les devis de livraison actuellement validés, sans les masquer dans le prix.</div><div className="space-y-3 border-b border-[#eadfd2] pb-5 text-sm"><div className="flex justify-between text-slate-600"><span>Sous-total articles</span><span className="font-medium text-slate-950">{formatPrice(subtotal, locale)}</span></div><div className="flex justify-between text-slate-600"><span>Frais de livraison validés</span><span className="font-medium text-slate-950">{isReadyForReview ? formatPrice(shipping, locale) : "À corriger"}</span></div></div><div className="flex justify-between py-5 text-xl font-bold"><span>Total estimé</span><span className="text-orange-600">{isReadyForReview ? formatPrice(estimatedTotal, locale) : "—"}</span></div><Button onClick={() => setLocation("/commander")} disabled={!isReadyForReview} className="h-12 w-full bg-orange-500 text-base font-semibold text-white hover:bg-orange-600 disabled:bg-slate-400">{isReadyForReview ? "Vérifier ma sélection" : "Destination à corriger"}</Button><p className="mt-3 text-center text-xs leading-5 text-slate-500">Aucun paiement, aucune adresse et aucune commande fournisseur ne sont demandés à cette étape.</p><Button onClick={() => setLocation("/boutique")} variant="outline" className="mt-3 w-full border-[#d9cbbc]">Continuer mes achats</Button><button onClick={() => { if (confirm("Êtes-vous sûr de vouloir vider votre panier ?")) clearCart(); }} className="mt-4 w-full text-xs font-semibold text-slate-400 hover:text-red-500">Vider le panier</button></CardContent></Card><div className="grid grid-cols-3 gap-2 text-center text-[11px] text-slate-500"><div className="border border-[#eadfd2] bg-white p-3"><PackageCheck className="mx-auto mb-2 h-4 w-4 text-orange-500" />Sélection vérifiée</div><div className="border border-[#eadfd2] bg-white p-3"><Truck className="mx-auto mb-2 h-4 w-4 text-orange-500" />Devis par pays</div><div className="border border-[#eadfd2] bg-white p-3"><LockKeyhole className="mx-auto mb-2 h-4 w-4 text-orange-500" />Paiement à venir</div></div></div></div></main><Footer /></div>;
}
